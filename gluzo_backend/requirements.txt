# Gluzo Backend — Python Dependencies
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.115.0
uvicorn>=0.30.0
gunicorn>=21.2.0

# Data Validation & Settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Agentic AI Framework
agno>=1.0.0

# Fuzzy String Matching (used for hybrid search & semantic cache)
rapidfuzz>=3.9.0

# Async HTTP Client (used for Tavily, Azure OpenAI Vision, and Deepgram Audio)
httpx>=0.27.0

# Database and ORM
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Search Engine and Vector DB
chromadb>=0.5.3
rank_bm25>=0.2.2

# LLM Providers (used by Agno)
openai>=1.0.0

# In-Memory Cache (used for TTL memory eviction)
cachetools>=5.3.0

# UI & HTTP
streamlit>=1.35.0
requests>=2.31.0

# API File Parsing & WebSockets
python-multipart>=0.0.9
websockets>=12.0

# Environment variables
python-dotenv>=1.0.0
